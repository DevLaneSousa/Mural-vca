Coloque aqui o arquivo muro-tijolos.png (foto/textura da parede de tijolos
usada como fundo do mural, tanto no /telao quanto no preview do /tablet).

Recomendações:
- Resolução mínima recomendada: 1920x1080 (ou a resolução real do telão do evento).
- Formato paisagem, sem elementos centrais importantes (o CSS usa
  background-size: cover, então as bordas podem ser cortadas dependendo da
  tela).
- Se quiser trocar o nome do arquivo, ajuste o caminho em
  client/src/styles.css, na regra .brick-wall (background-image: url(...)).
